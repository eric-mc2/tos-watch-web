---
layout: base
permalink: /index.html
title: 'ToS Watch'
description: 'Summaries and ratings of changes to consumer terms of service agreements.'
blog:
  title: 'Blog'
  intro: "Might re-use this for policies or services list..."
---

<div class="wrapper">
  <header class="full | section" style="--spot-color: var(--color-primary)">
    <div class="section__inner flow region">
      <h1 class="text-center" style="color: var(--color-light);">{{ title }}</h1>
    </div>
    <!-- {% svg "divider/waves", null, "divider" %} -->
  </header>

  <article class="full | region">
    <div class="prose wrapper flow">
      <h2 id="an-eleventy-starter">What do I want to write?</h2>
      <p>
        Maybe talk about the project in a paragrph here?
      </p>
      <p>
        <strong>Or</strong> talk about something else.
        You can <a href="/about/">link</a> to stuff.
      </p>
    </div>
  </article>

  <section class="full | region">
    <div class="prose wrapper flow">
      <h2>{{ blog.title }}</h2>
      {% if blog.intro %}
        <p>{{ blog.intro }}</p>
      {% endif %}
      <div class="feature |  region region-space-l">
        <custom-masonry layout="50-50">
          {% set itemList = collections.allPosts %}
					{% asyncEach item in itemList.slice(0, 4) %}
            {% set headingLevel = "h3" %}
						{% include "partials/card-blog.njk" %}
					{% endeach %}
        </custom-masonry>
      </div>
    </div>
  </section>
</div>
